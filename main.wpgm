import wollok.game.*
import paquetes.*
import clientes.*
import repartidor.*
import menu.*

program JuegoRepartidor {
    game.title("Juego del Repartidor")
    game.width(10)
    game.height(10)
    game.cellSize(50)
    menuInicial.mostrarMenu()
    game.start()
}